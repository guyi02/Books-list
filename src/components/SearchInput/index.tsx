import { Box, Input } from '@chakra-ui/react';

const SearchInput = () => {
  return (
    <Box w='100%'>
      <Input placeholder='Search for book' />
    </Box>
  );
};

export default SearchInput;
